# @http

The HTTP layer of [Tiny Internet](../../README.md).

This package parses requests and writes responses so BRAVO can serve `pi.world/` as a static page. It runs on BRAVO.

Spec: [Stage 3 · HTTP](../../docs/1-networking-fundamentals/3-http-server.md).

Fixtures and capture scripts live in [`src/fixtures/README.md`](./src/fixtures/README.md).
